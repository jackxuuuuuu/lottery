App({
  globalData: {
    // Base URL of the backend API.
    // Replace with your actual deployed server address before publishing.
    // This domain must also be added to the WeChat Mini Program's request whitelist
    // in the WeChat Official Accounts Platform → Development → Development Settings → Server Domain.
    apiBaseUrl: 'https://your-server.example.com'
  }
})
